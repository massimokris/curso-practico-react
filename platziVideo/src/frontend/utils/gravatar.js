import md5 from "md5";

const gravatar = (email) => {
  const base = "https://gravatar.com/avatar/";
  // const formattedEmail = email.trim().toLowerCase();
  // const hash = md5(formattedEmail, { endcoding: "binary" });
  // console.log(hash);
  // return `${base}${hash}`;
};

export default gravatar;
