import React from "react";
import "../assets/styles/components/NotFound.scss";

const NotFound = () => (
  <section className="main--not-found">
    <h2 className="main__title--not-found">440</h2>
    <p className="main__subtitle--not-found">Página no encontrada.</p>
    <a href="/">Regrese al inicio</a>
  </section>
);

export default NotFound;
